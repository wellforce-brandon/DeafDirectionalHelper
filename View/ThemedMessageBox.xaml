<Window x:Class="DeafDirectionalHelper.View.ThemedMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message"
        Width="400" Height="180"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5E5E5E"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="90"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="16,8"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <TextBlock Grid.Row="0" x:Name="MessageText" Text="Message"
                   FontSize="16" Foreground="White" TextWrapping="Wrap"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   TextAlignment="Center"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center">
            <!-- OK Button (for simple messages) -->
            <Button x:Name="OkButton" Content="OK" Click="OK_Click" Visibility="Collapsed"/>

            <!-- Yes/No Buttons (for confirmations) -->
            <Button x:Name="YesButton" Content="Yes" Click="Yes_Click" Visibility="Collapsed"
                    Background="#2E7D32" BorderBrush="#388E3C"/>
            <Button x:Name="NoButton" Content="No" Click="No_Click" Visibility="Collapsed" Margin="12,0,0,0"/>

            <!-- Yes/No/Cancel Buttons -->
            <Button x:Name="CancelButton" Content="Cancel" Click="Cancel_Click" Visibility="Collapsed" Margin="12,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>
