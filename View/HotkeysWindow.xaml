<Window x:Class="DeafDirectionalHelper.View.HotkeysWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keyboard Shortcuts"
        Width="400" Height="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3E3E3E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#5E5E5E"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="MinHeight" Value="44"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#4E4E4E"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#7E7E7E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#5E5E5E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Keyboard Shortcuts" FontSize="20" FontWeight="Bold"
                   Foreground="#88CCFF" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Hotkeys Grid -->
        <Grid Grid.Row="1" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Ctrl+Shift+R" FontFamily="Consolas" Foreground="#88CCFF" FontSize="16" Margin="0,8"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Text="Toggle enable/disable" Margin="20,8,0,8"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Ctrl+Shift+M" FontFamily="Consolas" Foreground="#88CCFF" FontSize="16" Margin="0,8"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="Toggle display mode" Margin="20,8,0,8"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Ctrl+Shift+S" FontFamily="Consolas" Foreground="#88CCFF" FontSize="16" Margin="0,8"/>
            <TextBlock Grid.Row="2" Grid.Column="1" Text="Show settings" Margin="20,8,0,8"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Ctrl+Shift+P" FontFamily="Consolas" Foreground="#88CCFF" FontSize="16" Margin="0,8"/>
            <TextBlock Grid.Row="3" Grid.Column="1" Text="Reset positions" Margin="20,8,0,8"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Ctrl+Shift+H" FontFamily="Consolas" Foreground="#88CCFF" FontSize="16" Margin="0,8"/>
            <TextBlock Grid.Row="4" Grid.Column="1" Text="Show hotkeys" Margin="20,8,0,8"/>
        </Grid>

        <!-- Close Button -->
        <Button Grid.Row="2" Content="Close" Click="Close_Click" HorizontalAlignment="Center" Margin="0,20,0,0"/>
    </Grid>
</Window>
